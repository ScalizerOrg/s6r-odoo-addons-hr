<?xml version="1.0" encoding="utf-8"?>
<!--
  ~ Copyright 2026 Scalizer (<https://www.scalizer.fr>)
  ~ License LGPL-3.0 or later (https://www.gnu.org/licenses/lgpl-3.0.html).
  -->
<odoo>

    <!-- Only offer the types the tags of the employee entitle them to. The domain
         attribute is replaced as a whole, the native conditions on the allocation
         being kept as they are. The dashboard and manager forms inherit this one,
         so they are covered too. -->
    <record id="hr_leave_view_form_employee_category" model="ir.ui.view">
        <field name="name">hr.leave.view.form.employee.category</field>
        <field name="model">hr.leave</field>
        <field name="inherit_id" ref="hr_holidays.hr_leave_view_form"/>
        <field name="arch" type="xml">
            <field name="holiday_status_id" position="before">
                <field name="allowed_holiday_status_ids" invisible="1"/>
            </field>
            <field name="holiday_status_id" position="attributes">
                <attribute name="domain">[
                    ('id', 'in', allowed_holiday_status_ids),
                    '|',
                        ('requires_allocation', '=', 'no'),
                        '&amp;',
                            ('has_valid_allocation', '=', True),
                            '|',
                                ('allows_negative', '=', True),
                                '&amp;',
                                    ('virtual_remaining_leaves', '&gt;', 0),
                                    ('allows_negative', '=', False),
                ]</attribute>
            </field>
        </field>
    </record>

</odoo>
